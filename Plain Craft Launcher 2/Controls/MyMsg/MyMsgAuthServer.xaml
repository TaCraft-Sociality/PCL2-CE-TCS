<Grid x:Class="PCL.MyMsgAuthServer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:my="clr-namespace:PCL"
      RenderTransformOrigin="0,0.5" UseLayoutRounding="True" SnapsToDevicePixels="True" MinWidth="400"
      HorizontalAlignment="Center" VerticalAlignment="Center" Margin="25">
    <Grid.RenderTransform>
        <TransformGroup>
            <RotateTransform x:Name="TransformRotate" Angle="-4" />
            <TranslateTransform x:Name="TransformPos" X="0" Y="40" />
        </TransformGroup>
    </Grid.RenderTransform>
    <Border Name="PanBorder" CornerRadius="7" Background="{DynamicResource ColorBrushBackground}">
        <Border.Effect>
            <DropShadowEffect Color="{DynamicResource ColorObjectMsgBoxShadow}" BlurRadius="20" ShadowDepth="2"
                              RenderingBias="Performance" Opacity="0.8" />
        </Border.Effect>
        <Grid Margin="22,22,22,23">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="2" />
                <RowDefinition Height="13" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" FontSize="23" Foreground="{DynamicResource ColorBrush2}"
                       HorizontalAlignment="Left" Name="LabTitle" Margin="7,-1,70,9" />
            <Rectangle x:Name="ShapeLine" Grid.Row="1" Height="2" Fill="{Binding Foreground, ElementName=LabTitle}" />
            <my:MyComboBox Grid.Row="4" x:Name="ComboServer" Height="28" IsEditable="True"
                           IsTextSearchEnabled="False" Margin="7,0,7,12" />
            <StackPanel Grid.Row="5" VerticalAlignment="Top" HorizontalAlignment="Right"
                        Margin="150,14,8,0" Orientation="Horizontal">
                <my:MyButton ColorType="Normal" x:Name="Btn1" Margin="12,0,0,0"
                             TextPadding="7" Padding="5,0" Click="Btn1_Click" />
                <my:MyButton ColorType="Normal" x:Name="Btn2" Margin="12,0,0,0"
                             TextPadding="7" Padding="5,0" Click="Btn2_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Grid>
